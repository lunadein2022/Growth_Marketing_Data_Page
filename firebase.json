{
  "firestore": {
    "rules": "server/firestore.rules",
    "indexes": "server/firestore.indexes.json"
  },
  "storage": {
    "rules": "server/storage.rules"
  },
  "functions": {
    "source": "functions"
  }
}
